" Override laststatus to always show the airline:
"   0: never
"   1: only if there are at least two windows
"   2: always
set laststatus=2

" Automatically displays all buffers when there's only one tab open.
let g:airline#extensions#tabline#enabled=1
